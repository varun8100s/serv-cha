{
  "name": "chatapp",
  "version": "1.0.0",
  "description": "a chat app using socket.io",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "devStart": "nodemon server.js",
    "start": "SET PORT=3000 && node server"
  },
  "author": "varun8100s",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.20"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "socket.io": "2.0"
  }
}
